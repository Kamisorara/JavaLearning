package _算法.leetcode.列表去重;

import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int T = scanner.nextInt();
        List<Integer> list = new ArrayList<>();
        int[] nums = new int[T+1];
        for (int i = 0; i < T; i++) {
            int num = scanner.nextInt();
            nums[i] = num;
        }


    }
}
